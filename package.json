{
  "private": true,
  "scripts": {
    "start": "lerna run --scope @cra-mfe/* --parallel start",
    "build": "lerna run --scope @cra-mfe/* build",
    "serve": "lerna run --scope @cra-mfe/* --parallel serve",
    "clean": "lerna run --scope @cra-mfe/* --parallel clean"
  },
  "devDependencies": {
    "lerna": "3.22.1"
  },
  "workspaces": {
    "packages": [
      "host",
      "app1"
    ]
  }
}
